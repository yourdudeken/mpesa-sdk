name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "New version to release (semver, e.g. 0.2.1)"
        required: true
        type: string

env:
  NEW_VERSION: ${{ inputs.version }}

jobs:
  release-ts:
    name: Publish TypeScript SDK
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org
      - run: npm ci
      - run: npm -w typescript version ${{ inputs.version }} --no-git-tag-version
      - run: npm run build:ts
      - run: npm -w typescript publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  release-python:
    name: Publish Python SDK
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install hatch
      - run: cd python && hatch version ${{ inputs.version }}
      - run: cd python && hatch build
      - run: cd python && hatch publish -u __token__ -a ${{ secrets.PYPI_TOKEN }}

  release-go:
    name: Tag Go Module
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - run: |
          git tag go/v${{ inputs.version }}
          git push origin go/v${{ inputs.version }}
